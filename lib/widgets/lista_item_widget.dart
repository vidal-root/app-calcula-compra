import 'package:flutter/material.dart';

class ListaItemWidgets extends StatelessWidget {
  const ListaItemWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}